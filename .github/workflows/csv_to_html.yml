name: Convert CSV to HTML

on:
  push:
    branches:
      - main

# 1️⃣ Autorise le GITHUB_TOKEN à écrire dans le dépôt
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 2️⃣ Récupérer le code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 3️⃣ Installer Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 4️⃣ Installer les dépendances
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # 5️⃣ Générer TEI depuis CSV
      - name: CSV to TEI
        run: python py/csv_to_xml.py

      # 6️⃣ Télécharger Saxon HE
      - name: Download Saxon HE
        run: |
          wget https://github.com/Saxonica/Saxon-HE/releases/download/SaxonHE12-9/SaxonHE12-9J.zip -O saxon.zip
          unzip saxon.zip -d saxon

      # 7️⃣ Transformer TEI → HTML
      - name: TEI to HTML with Saxon
        run: |
          mkdir -p data
          for tei_file in tei/*.xml; do
            filename=$(basename "$tei_file" .xml)
            java -cp "saxon/saxon-he-12.9.jar:saxon/lib/*" net.sf.saxon.Transform \
              -s:"$tei_file" \
              -xsl:xsl/xml_to_html.xsl \
              -o:"data/${filename}.html"
          done

      # 8️⃣ Commit & push les fichiers XML et HTML
      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Désactive l'affichage des codes octaux pour les accents (ex: \303)
          git config --global core.quotePath false
          
          # On ajoute spécifiquement les fichiers générés
          # Cela inclut les nouveaux fichiers dans tei/ et data/
          git add data/*.html
          git add tei/*.xml
          
          # On vérifie s'il y a des changements réels à committer
          if [ -z "$(git status --porcelain)" ]; then
            echo "Aucune modification détectée."
          else
            git commit -m "Update TEI and HTML files from CSV [skip ci]"
            git push origin main
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
